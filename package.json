{
  "name": "chat-box-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm ci --prefix backend --ignore-scripts && npm ci --prefix frontend --ignore-scripts && npm run build --prefix frontend",
    "start": "node backend/server.js",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend"
  }
}
